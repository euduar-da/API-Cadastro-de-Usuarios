# --------------------
# Adições Necessárias para o Seu Projeto
# --------------------

# 1. Arquivos de Banco de Dados
# O arquivo de DB (SQLite) que é persistido na sua máquina local.
*.db
*.sqlite
*.sqlite3

# Arquivos de dados específicos (que você já subiu e precisa limpar)
meu_banco.db
usuarios.db

# 2. Arquivos de Log e Temporários
*.log
/tmp/

# 3. Docker (Opcional, mas recomendado para logs e binários)
# Exclui arquivos de build temporários do Docker (swp)
*.swp